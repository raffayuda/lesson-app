{
  "name": "svelte-express-app",
  "version": "1.0.0",
  "description": "Role-based Attendance System with Svelte + Express",
  "main": "api/index.js",
  "scripts": {
    "dev": "node api/index.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma migrate deploy",
    "prisma:seed": "node prisma/seed.js",
    "build": "cd frontend && npm run build",
    "vercel-build": "prisma generate && prisma migrate deploy && cd frontend && npm install && npm run build"
  },
  "keywords": [
    "svelte",
    "express",
    "prisma",
    "postgresql",
    "attendance"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.7.1",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "node-telegram-bot-api": "^0.67.0"
  },
  "devDependencies": {
    "prisma": "^5.7.1"
  }
}
